# 引き出し線がラベルを横切る箇所の経過観察リスト

crossSection の `leaders[]` で、引き出し線が自分の `labelMain` / `labelSub` の
文字の上を通っている箇所の記録。

## 背景

引き出し線はラベルのすぐ横で終わるため、線がラベルの外接矩形をかすめること自体は
構造上ほぼすべての図で起きる(2026-08-25 時点で全 102 記事を実測して 125 箇所)。
そのため「かすめている/いない」ではなく、**横断率**(ラベルの幅に対して、線が
文字帯の中を通る水平距離の割合)で実害を切り分けている。

- **50% 以上** … 判読に支障が出る水準。修正対象(2026-08-25 に 8 件を是正済み)
- **50% 未満** … 実害は小さい。このリストで経過観察とし、**その図を別件で触る際に
  ついでに解消する**方針(単独では修正しない)

判定基準の根拠: 座りっぱなしの脚のむくみ記事で横断率 0.38 の箇所を目視確認した際、
重なりに気づけなかった。40% 前後は実用上問題にならないと判断した。

## 測り方

`scripts/verify-rendered-layout.js` とは別に、実ブラウザ(幅 768)で
`line.chart-diagram-leader` と同一 SVG 内の `<text>` の実測 bbox を取り、
Liang-Barsky で線分の矩形内区間を求めて `区間の水平幅 ÷ ラベル幅` を算出している。
検査スクリプト本体には組み込んでいない(item35 は引き出し線を意図的に除外している)。

## 経過観察リスト(2026-08-25 時点、横断率 0.35〜0.49)

| 記事 | ラベル | 横断率 |
|---|---|---|
| 2026-08-24_湿気で髪型が崩れる仕組み | 形が変わる | 0.48 |
| 2026-08-25_目もとを温めるケアと学会の推奨 | 目の表面をおおう層 | 0.47 |
| 2026-08-24_ブラッシングで髪が傷むときの見直し方 | 毛先側 | 0.47 |
| 2026-08-24_香水はつける位置で香り方が変わる | 歩くと下から立ちのぼる | 0.41 |
| 2026-08-23_座りっぱなしの脚のむくみと歩数の話 | 夕方に張りを感じやすい部分 | 0.38 |
| 2026-08-23_サングラスは色の濃さで選ばない | 網膜 | 0.38 |

横断率 0.35 未満の箇所は件数が多く(閾値なしで 125 箇所)、crossSection の設計に
由来するため一覧化していない。

## 是正のしかた

終点 `toX` / `toY` を始点側へ寄せ、**最終接近の傾きを立てる**のが基本。
ラベルの幅を W、文字帯の高さを約 12 とすると、横断率はおよそ `12 ÷ (傾き × W)`
になるため、`傾き > 24 ÷ W` を満たせば 50% を下回る。

傾きを変えたことで別の図形と新たに重なる場合は、ラベル群ごと空きスペースへ
移動する(2026-08-25 のヘアアイロン記事・マッサージ機器記事はこの方法)。
移動後は必ず `verify-rendered-layout.js` の item35 で text-shape の重なりが
増えていないかを確認すること。

---

# フォント幅の余裕が小さい図(余裕10〜20%)

## 背景

CIのフォント(IPAGothic)がローカル(Yu Gothic)より 3.0〜4.4% 幅広で、
ローカルで0件でもCIでのみ検出が出る事象が起きた(2026-08-25)。原因は
フォント差そのものではなく、**余白が数%しかない図が複数あったこと**との
組み合わせだった。

そこで全195記事のSVGテキストについて「フォントがあと何%太ると破綻するか」
(= 余裕%)を機械的に測り、**余裕10%未満の16件を余白拡張で是正済み**。
残る 10〜20% の項目をこのリストで経過観察する。

- **10%未満** … 修正対象。フォントが変わっても再発しない水準まで余白を広げる
- **10〜20%** … 経過観察。単独では修正せず、**その図を別件で触る際にあわせて広げる**
- **20%以上** … 対応不要

## 測り方

記事ページを実ブラウザ(幅 320 / 375 / 768)で開き、SVG内の `<text>` の
`font-size` を 1.00〜1.30 倍まで段階的に拡大して、はみ出し(overflow)・
図形との接触(text-shape)・テキスト同士の重なり(collision)が発生する
倍率を求める。判定の幾何は `scripts/verify-rendered-layout.js` の item35 に
合わせている(TOL=3、同一 `<g>` 除外、引き出し線除外、`data-allow-overlap`
除外、`elementsFromPoint` による実インク確認)。3幅の最小値を採用する。

検査スクリプト本体には組み込んでいない(調査用の外部ツール)。

## 経過観察リスト(2026-08-25 の是正後、余裕10〜20%の20件)

余裕%は**是正後の再スキャン値**。是正前は26件だった。

| 余裕 | 種類 | 幅 | 記事 | 図 | ラベル |
|---|---|---|---|---|---|
| 10% | overflow | 320 | 2026-08-05_くせ毛タイプ別まとまる髪型ガイド | svg#1 | 根元は素直で毛先だけピンとハネる → 毛先ハネタイプ |
| 10% | text-shape | 768 | 2026-08-08_大人の汗疹あせも対策 | svg#2 | ジュクジュク・境界が |
| 15% | overflow | 320 | 2026-08-09_寝つきが悪い夜のナイトルーティン | svg#1 | 深部体温がいったん上がり、そのあと下が |
| 15% | overflow | 320 | 2026-08-24_巻き髪がすぐ取れる理由 | svg#2 | ブロッキングし、毛流れと面をそろえる |
| 15% | overflow | 768 | 2026-07-20_夏の皮脂汗メイク崩れ対策 | svg#0 | 「乾燥」に✓が多い |
| 15% | overflow | 768 | 2026-07-20_夏の皮脂汗メイク崩れ対策 | svg#0 | 「皮脂」に✓が多い |
| 15% | overflow | 768 | 2026-07-21_メイクとスキンケアの相性 | svg#0 | 化粧水→美容液 |
| 15% | overflow | 768 | 2026-08-08_大人の汗疹あせも対策 | svg#1 | ①正常 |
| 15% | overflow | 768 | 2026-08-08_大人の汗疹あせも対策 | svg#1 | ②詰まる |
| 15% | overflow | 768 | 2026-08-08_大人の汗疹あせも対策 | svg#1 | ③炎症 |
| 15% | text-shape | 320 | 2026-07-26_基礎化粧品の基本知識と肌質診断 | svg#4 | 1. クレンジング |
| 15% | text-shape | 320 | 2026-07-26_基礎化粧品の基本知識と肌質診断 | svg#4 | 6. クリーム |
| 15% | text-shape | 320 | 2026-08-02_前髪の巻き方完全ガイド | svg#2 | 太い・硬い髪 |
| 15% | text-shape | 320 | 2026-08-09_パサつく髪の補修トリートメントの選び方 | svg#0 | 髪の表面(キューティクル) |
| 15% | text-shape | 320 | 2026-08-13_白髪ぼかしハイライトの仕組み | svg#0 | 色がつく部分 |
| 15% | text-shape | 320 | 2026-08-24_香水はつける位置で香り方が変わる | svg#0 | 脈打つ箇所は体温が高い |
| 15% | text-shape | 375 | 2026-08-02_頭皮かゆみフケシャンプー比較 | svg#1 | 大きくベタつく |
| 15% | text-shape | 375 | 2026-08-02_頭皮かゆみフケシャンプー比較 | svg#1 | 細かくパラパラ |
| 15% | text-shape | 375 | 2026-08-05_オイルシャンプーとノンシリコンシャンプーの違い | svg#1 | 髪の表面(キューティクル) |
| 15% | text-shape | 375 | 2026-08-25_脱毛器の家庭用と医療用の線引き | svg#0 | 破壊する行為は医行為とされる |

## 注意: 診断フロー型・折れ線型はスキャン値が甘く出る

`diagnosis` 系(`kssDiagnosisFlow` `asemoDiagnosisFlow` `makeupDiagnosisFlow` 等)と
`lineChart` は、フォントが太っても枠やプロット領域の座標が固定されたままで、
テキストだけが伸びる。そのため等倍拡大で測る余裕%は、実際の環境差
(別フォントでの字幅の偏り)を完全には再現しない。

これらの型については、**再スキャンの数値だけで合格としない**こと。
是正時は幅 320 / 375 の実描画で接触が消えていることを個別に確認する
(PMS記事の折れ線図はこの手順で確認済み)。

## steps型を触るときの注意(行頭禁則)

`renderGenericStepsHtml` は `splitLabelForWrap` で1行あたりの文字数を
超える文字列を2行に折り返す。折り返し位置は文字列の中央付近で、
`breakChars` に含まれる文字があればそこを優先する。

2026-08-25 に1行の文字数見積もり(`CHAR_UNIT`)を 11.54 → 13.3
(21字 → 18字)へ引き上げた際、それまで1行に収まっていた文字列が
折り返されるようになり、**「。」が次の行の先頭に落ちる行頭禁則違反**が
発生した。`breakChars` に `。 ! ? ！ ？` を追加して解消している。

今後 `CHAR_UNIT` や `textX` を変える場合は、余裕%だけでなく
**折り返し位置が変わる記事の実描画**(行頭に句読点・閉じ括弧が
落ちていないか)まで確認すること。

### 改善候補: カタカナ語の途中で改行される(2026-08-25 記録、未実装)

`splitLabelForWrap` は文字列の中央付近を分割位置とし、その前後2文字以内に
`breakChars`(`の ・ ( （ 空白 、 。 ! ? ！ ？`)があればそこを優先する。
語の切れ目は見ていないため、区切り文字が近くに無いとカタカナ語の途中で
改行されることがある。

- **発生条件**: 1行の上限(現行18字)を超える文字列で、中央±2文字に
  `breakChars` が1つも無い場合。実例は `2026-08-25_PSEマークとリコールの確認`
  の「メーカー名と型番でリ / コール情報を調べる」
- **実害**: はみ出し・重なりではなく、読みにくさのみ。前フェーズの仕分け基準では
  「実害なし」に相当する
- **対応案**: `breakChars` による優先探索の前に「カタカナ(および英数字)の連続の
  途中は分割しない」という判定を足し、連続の境界まで分割位置をずらす。
  ずらせる範囲に上限を設けないと極端に不均等な2行になるため、
  中央±4文字程度に制限する必要がある
- **波及の見積もり**: 2026-08-25 の28記事是正後、steps図は55個・折り返しが
  発生する記事は30本ある。そのうち**分割位置がカタカナ連続内に落ちる候補は3本**
  (`2026-08-09_寝つきが悪い夜のナイトルーティン`「リラックス」/
  `2026-08-24_純アルコール量で自分の飲み方を見る`「アルコール」/
  `2026-08-25_PSEマークとリコールの確認`「リコール」)。ただし分割位置を
  ずらすと片方の行が長くなるため、**steps使用の全55図で余裕%の再測定と、
  折り返しが変わる記事の 320/375/768px 描画確認が必要**。単独で着手する
  規模ではなく、steps型のレンダラを別件で触る際にあわせて検討する


---

# その他の既知の未対応事項

## ITEM35_IMAGE_CROP_RISK(サムネイル画像の切れ)

`verify-rendered-layout.js` の item35 が出す非ブロッキングの検出。
`object-fit: cover` で表示している画像が、元画像の何%を切り落としているかを見る
(閾値は `CROP_RISK_THRESHOLD_PCT = 5`)。文字や図形の重なりとは別系統の指摘。

| 記事 | 画像 | 軸 | 切り落とし | 元サイズ |
|---|---|---|---|---|
| 2026-08-24_髪を結ぶ位置と根元にかかる力 | 2026-08-24_髪を結ぶ位置と根元にかかる力.webp | 縦 | 15.6% | 1536×1024 |

2026-08-25 のバッチ4(引き出し線)対応時に検出されたが、**今回の対象外**として
未対応のまま残している。図解の座標調整では解消しない(画像側の差し替え、または
表示比率の見直しが必要)ため、画像を扱う作業のときにあわせて判断する。
検出は幅 320 / 375 / 768 のすべてで同じ 15.6% であり、特定の画面幅に限った問題ではない。
